{% extends "base.html.twig" %}

{% block body %}

    <h1>Créer une liste</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Valider</button>
    {{ form_end(form) }}

{% endblock %}