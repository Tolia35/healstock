<article> {# list info #}
    <a style="color: black" href="{{ path('checklist_show', {'id': checklist.id}) }}">
        N°{{ checklist.id }} - {{ checklist.nursename }} - {{ checklist.createtime|date('d/m/Y') }}
    </a>
</article>